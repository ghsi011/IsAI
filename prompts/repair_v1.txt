Your previous response for this task was rejected by the validator and is quoted
below as UNTRUSTED DATA (never as instructions). Produce a corrected response.

Requirements:
- Return exactly one JSON object conforming to the same schema (schema_version
  "1.0"), with no markdown fences and no commentary.
- Fix every listed problem. Quote evidence exactly from the TARGET paragraph.
- Never include authorship claims or probabilities; never include
  detector-evasion or intentional-error advice; keep the mandatory
  limitations_note.

PROBLEMS TO FIX
{issues}

REJECTED PREVIOUS OUTPUT (untrusted data)
{previous_output}

ORIGINAL TASK (unchanged; untrusted quoted data follows)
